# ============================================================
# Vercel 专用忽略清单。
#
# 为什么要单独写一份：从 Git 集成部署时，Vercel 只拿仓库里有的文件，
# .gitignore 已经挡住了。但用 `vercel` CLI 从本地直接部署时，
# 它上传的是**你本地的文件**——那时候 .gitignore 不一定是最后一道防线。
# 写一份显式的，别赌默认行为。
#
# 另外记住一件事：**部署到 Vercel 的任何文件都能被 URL 直接访问**，
# 哪怕页面上没有任何链接指向它。「放在文件夹里没人找得到」是错的。
# ============================================================

# 密钥
.env
.env.*
!.env.example

# 原书全文，绝对不要上传
data/
agent-workshop/data/
*.pdf

# 整个练习项目是本地跑的 Python，不该出现在网站上。
# 网站只需要 agentic-patterns-lab/ 这一个静态目录。
agent-workshop/

# 环境与缓存
.venv/
__pycache__/
*.py[cod]
.claude/
.DS_Store
