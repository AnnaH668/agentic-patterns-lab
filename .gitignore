# ============================================================
# 开源前的第一道闸门。子目录里的 .gitignore 只管自己那一层，
# 这个根文件管全部——两个都留着，是故意的冗余。
# ============================================================

# ---- 密钥。这一条永远排第一 ----
# 注意：.gitignore 不支持**行尾注释**——写在模式后面的 # 会被当成模式的一部分。
# 所以注释必须单独一行，否则 !.env.example 会变成一条谁也匹配不上的废规则。
.env
.env.*
# 模板要保留：它里面只有占位符，没有真 key
!.env.example

# ---- 原书内容：**不要发布** ----
# data/book_chunks.json 是《Agentic Design Patterns》482 页的逐字全文
# （94 万字符），只是被切成了块。把它推上 GitHub 等于公开转载整本书。
# 别人克隆之后自己跑 step0 重新生成即可，只需要他们自己那份 PDF。
data/
agent-workshop/data/
*.pdf

# ---- 环境与缓存 ----
.venv/
venv/
__pycache__/
*.py[cod]
node_modules/

# ---- 本地工具配置 ----
# 整个目录都不发布：里面只有本机的开发服务器参数和权限设置，
# 对访客没有意义，却会占据文件列表的第一行。
# 注意是「不再跟踪」而非删除——本地文件保留，开发环境照常可用。
.claude/

# ---- 只给自己看的本地笔记 ----
*.local.md

# ---- 系统垃圾 ----
.DS_Store
Thumbs.db
